$("#comments_area").append(
  '<p class="left"><%= @comment.user.name %>さんがコメントしました。</p><p class="left"><%= @comment.content %></p><% if current_user.id == @comment.user_id %><p class="right"><a class="edit fa fa-pencil-square-o fa-lg"></a><%= link_to '', comment_path(@comment), class: "fa fa-trash-o fa-lg", method: :delete, data: { confirm: '本当に削除していいですか？' } %></p><% end %>')
$(':text').val('')
