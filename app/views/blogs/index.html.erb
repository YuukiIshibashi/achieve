<div class="container">
<div class="wrapper col-md-8 col-md-offset-2 col-sm-10">
<p><%= notice %></p>

<h1>ブログ一覧</h1>
<%= link_to "ブログを作成する", new_blog_path, class: 'btn btn-default' %>
<% @blogs.each do |blog| %>
 <div class="row">
   <%= link_to blog_path(blog) do %>
    <h4><strong><%= blog.title %></strong></h4>
    <% end %>
  <p class="user_img"><%= profile_img(blog.user)%></p><br>
  <small>ユーザID:<%= blog.user_id %></small>
  <small>名前:<%= blog.user.name %></small>


  <p>本文:<%= blog.content %></p>
  <%= link_to "編集", edit_blog_path(blog.id), class: 'btn btn-default btn-sm btn-success' %>
  <%= link_to "削除", blog_path(blog), method: :delete, data:{confirm: "本当に削除していいですか？"}, class: 'btn btn-default btn-sm btn btn-danger' %>
 </div>
<% end %>
<br>
<div id="gototop">
 <div id="gototop2"></div>
</div>
</div>1
</div>
